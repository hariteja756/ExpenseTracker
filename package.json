{
  "name": "expense-tracker-fullstack",
  "version": "1.0.0",
  "description": "Full-stack Expense Tracker with sportswear brand aesthetic",
  "main": "server/server.js",
  "type": "module",
  "scripts": {
    "install-all": "npm install --include=dev --prefix client && npm install --include=dev --prefix server",
    "build-client": "npm run build --prefix client",
    "build": "npm run install-all && npm run build-client",
    "start": "node server/server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongodb-memory-server": "^11.2.0",
    "mongoose": "^8.4.1",
    "morgan": "^1.10.0"
  }
}
